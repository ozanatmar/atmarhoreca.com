# Claude Code Instructions

## Branch Policy

- **All changes must be pushed to the `dev` branch** — never to `main` or `master` unless explicitly requested.
- Only push to `main` when the user specifically says to (e.g., "push to main", "merge to main", "deploy to main").
- When committing and pushing, always target `origin/dev`.
